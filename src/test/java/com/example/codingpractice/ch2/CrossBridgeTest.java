package com.example.codingpractice.ch2;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class CrossBridgeTest {

    @Test
    void solution() {

/*        CrossBridge crossBridge = new CrossBridge();
        Assertions.assertEquals(8, crossBridge.solution(2, 10, new int[]{7,4,5,6}));
        Assertions.assertEquals(101, crossBridge.solution(100, 10, new int[]{10}));
        Assertions.assertEquals(100, crossBridge.solution(100, 10, new int[]{10,10,10,10,10,10,10,10,10,10}));*/
    }
}
